
server:
  port: 5555

eureka:
  client:
    service-url:
      defaultZone: http://peer1:1111/eureka/,http://peer2:1112/eureka/

spring:
  application:
    name: api-gateway

zuul:
  routes:
    api-a:
      path: /api-a/**
      url: hello-service
    api-b:
      path: /api-b/**
      url: feign-consumer
